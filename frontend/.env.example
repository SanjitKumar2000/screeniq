# Frontend environment — copy to .env.local

# Public — exposed to the browser. Only the API base URL goes here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Server-only secrets (none right now; JWTs are user-scoped, stored client-side).
